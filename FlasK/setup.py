from setuptools import setup

setup(
    name='FlasK',
    packages=['FlasK'],
    include_package_data=True,
    install_requires=[ 's',
    ],
)

"""
CMD:
set FLASK_APP=FlasK - musisz być w folderze projektu
"""